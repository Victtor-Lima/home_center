import React from "react";
import styles from "../Style/InputSearch.module.css";

const InputSearch = () => {
  return <input className={styles.input_search} type="text" />;
};

export default InputSearch;
