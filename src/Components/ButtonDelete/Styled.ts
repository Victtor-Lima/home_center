import styled from "styled-components";

export const ButtonDelete = styled.button`
  justify-self: right;
  color: ${(props) => props?.color};
  padding: 5px 15px;
  border: none;
  border-radius: 8px;
`;
